---
import { ViewTransitions } from "astro:transitions";
import MoreTools from "../info/moretools.astro";
import Footer from "./Footer.astro";
import Head from "./Head.astro";
import Header from "./header.astro";
import "../../styles/globals.css";
import FormatHeading from "../FormatHeading.astro";

type Props = {
  title: string;
  description: string;
  language: string;
  image?: string;
};

const { title, description, image, language } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <Head
      title={`${title} | Happy Formatter, the best online formatter`}
      description={description}
      image={image}
    />
    <ViewTransitions />
  </head>
  <body>
    <Header />
    <main>
      <FormatHeading language={language} />
      <slot />
    </main>
    <MoreTools />
    <Footer />
  </body>
</html>
