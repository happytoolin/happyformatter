---
import Info from "@/components/info/info-enhanced.astro";
import ReactIsland from "@/components/islands/ReactIsland.astro";
import Layout from "@/components/layout/layout.astro";
import { type LanguageConfig, LANGUAGES } from "@/lib/languages";
import { buildToolPageSEO } from "@/lib/tool-page-seo";
import type { GetStaticPaths } from "astro";

export const getStaticPaths: GetStaticPaths = (async () => {
  const paths: any[] = [];

  // Generate paths only for languages that support minification
  Object.entries(LANGUAGES).forEach(([langId, langConfig]) => {
    if (langConfig.minify) {
      paths.push({
        params: { lang: langId },
        props: {
          language: langId,
          languageConfig: langConfig,
          lang: langId,
        },
      });
    }
  });

  return paths;
}) satisfies GetStaticPaths;

interface Props {
  language: string;
  languageConfig: LanguageConfig;
  lang: string;
}

const { language, languageConfig, lang: _lang } = Astro.props;

// Parse the lang parameter to detect hyphenated variants
const langParam = _lang;
const hyphenatedParts = langParam ? langParam.split("-") : [];
let actualLanguage = language;
let variant = null;
let actualVariantData = null;

if (hyphenatedParts.length >= 2) {
  // This might be a hyphenated variant like "javascript-biome"
  const potentialBaseLanguage = hyphenatedParts[0];
  const potentialVariant = hyphenatedParts.slice(1).join("-");

  // Check if the base language exists and the variant is valid for it
  if (LANGUAGES[potentialBaseLanguage]) {
    const { getLanguageVariant } = await import("@/lib/seo-variants");
    const variantData = getLanguageVariant(
      potentialBaseLanguage,
      potentialVariant,
    );

    if (variantData) {
      actualLanguage = potentialBaseLanguage;
      variant = potentialVariant;
      actualVariantData = variantData;
    }
  }
}

const actualLanguageConfig = LANGUAGES[actualLanguage] || languageConfig;
const runtimeFormatterVariants = new Set(["biome", "ruff", "mago"]);
const formatterLanguage = variant && runtimeFormatterVariants.has(variant)
  ? `${actualLanguage}-${variant}`
  : actualLanguage;

// Safety checks
if (!actualLanguage || !actualLanguageConfig) {
  throw new Error(
    `Language or languageConfig is undefined. Language: ${actualLanguage}`,
  );
}

// Check if this language actually supports minification
if (!actualLanguageConfig.minify) {
  throw new Error(`${actualLanguageConfig.name} does not support minification`);
}

const toolSEO = buildToolPageSEO({
  language: actualLanguage,
  languageConfig: actualLanguageConfig,
  minify: true,
  variant,
  variantData: actualVariantData,
});
const displayVariantData = variant ? { h1: toolSEO.h1 } : null;
---

<Layout
  title={toolSEO.title}
  description={toolSEO.description}
  language={actualLanguage}
  minify={true}
  canonicalPath={toolSEO.canonicalPath}
  keywords={toolSEO.keywords}
  category={toolSEO.category}
  currentVariant={variant}
  breadcrumbs={toolSEO.breadcrumbs}
  faqItems={toolSEO.faqItems}
  showFormatHeading={false}
  structuredDataName={toolSEO.schemaName}
  structuredDataDescription={toolSEO.schemaDescription}
>
  <div class="mx-auto max-w-[1400px] px-4 py-6 sm:px-6 lg:px-8">
    <div class="mb-6 border-y border-border py-5">
      <div class="mb-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
        <div class="flex flex-wrap items-center gap-3">
          <h1 class="text-2xl font-semibold text-foreground md:text-4xl">
            {toolSEO.h1}
          </h1>
          <span
            class="rounded-md border border-border bg-card px-2.5 py-1 text-sm text-muted-foreground"
          >
            Minify
          </span>
        </div>
      </div>
      <div class="mb-4 max-w-2xl">
        <p class="font-medium leading-relaxed text-foreground">
          {toolSEO.description}
        </p>
      </div>
      <div class="flex items-center gap-4">
        <a
          href={`/${actualLanguage}${variant ? `-${variant}` : ""}`}
          class="inline-flex items-center gap-2 rounded-md border border-border bg-card px-3 py-2 text-sm font-medium text-foreground transition-[background-color,border-color,transform] duration-150 ease-out hover:border-foreground hover:bg-secondary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring active:translate-y-px motion-reduce:transition-none"
        >
          Back to {actualLanguageConfig.name}
          {variant ? ` ${variant.charAt(0).toUpperCase() + variant.slice(1)}` : ""}
          Tool
        </a>
        <div class="hidden items-center gap-2 text-muted-foreground md:flex">
          <span class="h-px w-6 bg-border"></span>
          <span class="text-sm">Runs locally</span>
        </div>
      </div>
    </div>
  </div>

  <ReactIsland
    kind="formatter"
    props={{ minifier: true, language: formatterLanguage }}
  />
  <Info
    language={actualLanguage}
    minify={true}
    variant={variant}
    variantData={displayVariantData}
  />
  <ReactIsland
    kind="faq-enhanced"
    props={{
      language: actualLanguage,
      minify: true,
      variant,
      variantData: displayVariantData,
    }}
  />
</Layout>
